export interface RequirementName {
    text: {
        [id: string]: string
    }
}

export interface RequirementSuffix {
    text: {
        [id: string]: string
    }
}

export const ZH_REQUIREMENT_NAME_CLASS = "职业：";